import {sqliteTable,text,integer,real,index,uniqueIndex,check} from 'drizzle-orm/sqlite-core';
import {sql} from 'drizzle-orm';
export const users=sqliteTable('users',{id:text().primaryKey(),email:text().notNull(),name:text().notNull(),role:text().notNull().default('customer')});
export const products=sqliteTable('products',{id:text().primaryKey(),sellerId:text().notNull().references(()=>users.id),name:text().notNull(),brand:text().notNull(),category:text().notNull(),description:text().notNull(),price:integer().notNull(),comparePrice:integer().notNull().default(0),stock:integer().notNull().default(0),image:text().notNull(),badge:text().notNull().default(''),active:integer().notNull().default(1),createdAt:text().notNull()},t=>[index('products_category_active').on(t.category,t.active),index('products_seller').on(t.sellerId),check('stock_nonnegative',sql`${t.stock} >= 0`),check('price_positive',sql`${t.price} > 0`)]);
export const cartItems=sqliteTable('cart_items',{owner:text().notNull(),productId:text().notNull().references(()=>products.id),quantity:integer().notNull()},t=>[uniqueIndex('cart_owner_product').on(t.owner,t.productId),check('cart_quantity',sql`${t.quantity} BETWEEN 1 AND 20`)]);
export const wishlists=sqliteTable('wishlists',{userId:text().notNull().references(()=>users.id),productId:text().notNull().references(()=>products.id)},t=>[uniqueIndex('wishlist_user_product').on(t.userId,t.productId)]);
export const addresses=sqliteTable('addresses',{id:text().primaryKey(),userId:text().notNull().references(()=>users.id),name:text().notNull(),phone:text().notNull(),line1:text().notNull(),city:text().notNull(),state:text().notNull(),postalCode:text().notNull(),country:text().notNull()},t=>[index('addresses_user').on(t.userId)]);
export const coupons=sqliteTable('coupons',{code:text().primaryKey(),percent:integer().notNull(),minimum:integer().notNull(),maximum:integer().notNull(),active:integer().notNull().default(1),expiresAt:text().notNull()});
export const orders=sqliteTable('orders',{id:text().primaryKey(),userId:text().notNull().references(()=>users.id),idempotencyKey:text().notNull(),subtotal:integer().notNull(),discount:integer().notNull(),shipping:integer().notNull(),total:integer().notNull(),currency:text().notNull().default('INR'),status:text().notNull().default('awaiting_payment'),paymentStatus:text().notNull().default('unpaid'),paymentProvider:text().notNull(),providerId:text(),address:text().notNull(),coupon:text(),tracking:text().notNull().default(''),createdAt:text().notNull(),deliveredAt:text(),expiresAt:text().notNull()},t=>[uniqueIndex('order_user_idempotency').on(t.userId,t.idempotencyKey),index('orders_user_created').on(t.userId,t.createdAt),index('orders_status').on(t.status)]);
export const orderItems=sqliteTable('order_items',{id:text().primaryKey(),orderId:text().notNull().references(()=>orders.id),productId:text().notNull().references(()=>products.id),sellerId:text().notNull().references(()=>users.id),name:text().notNull(),price:integer().notNull(),quantity:integer().notNull(),image:text().notNull()},t=>[index('items_order').on(t.orderId),index('items_seller').on(t.sellerId)]);
export const reviews=sqliteTable('reviews',{id:text().primaryKey(),userId:text().notNull().references(()=>users.id),productId:text().notNull().references(()=>products.id),rating:integer().notNull(),body:text().notNull(),createdAt:text().notNull()},t=>[uniqueIndex('review_user_product').on(t.userId,t.productId),check('review_rating',sql`${t.rating} BETWEEN 1 AND 5`)]);
export const notifications=sqliteTable('notifications',{id:text().primaryKey(),userId:text().notNull().references(()=>users.id),message:text().notNull(),read:integer().notNull().default(0),createdAt:text().notNull()},t=>[index('notifications_user').on(t.userId)]);
export const returns=sqliteTable('returns',{id:text().primaryKey(),orderId:text().notNull().references(()=>orders.id),userId:text().notNull().references(()=>users.id),reason:text().notNull(),status:text().notNull().default('requested'),createdAt:text().notNull()},t=>[uniqueIndex('return_order').on(t.orderId)]);
export const sellerApplications=sqliteTable('seller_applications',{id:text().primaryKey(),userId:text().notNull().references(()=>users.id),storeName:text().notNull(),description:text().notNull(),status:text().notNull().default('pending'),createdAt:text().notNull()},t=>[uniqueIndex('seller_application_user').on(t.userId)]);
export const messages=sqliteTable('messages',{id:text().primaryKey(),userId:text().notNull().references(()=>users.id),subject:text().notNull(),body:text().notNull(),createdAt:text().notNull()});
export const outbox=sqliteTable('outbox',{id:text().primaryKey(),userId:text().notNull(),subject:text().notNull(),body:text().notNull(),status:text().notNull().default('pending'),createdAt:text().notNull()});
export const webhookEvents=sqliteTable('webhook_events',{id:text().primaryKey(),createdAt:text().notNull()});
export const rateLimits=sqliteTable('rate_limits',{key:text().primaryKey(),count:integer().notNull(),expiresAt:integer().notNull()});
export const auditLog=sqliteTable('audit_log',{id:text().primaryKey(),actor:text().notNull(),action:text().notNull(),target:text().notNull(),createdAt:text().notNull()});
